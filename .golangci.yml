version: "2"

run:
  timeout: 5m

issues:
  exclude-dirs:
    - templates
    - "templates/.*"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
